[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "threadvault"
version = "0.31.0"
description = "Local-first Codex session archive CLI."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "ThreadVault contributors" }]
dependencies = [
  "jinja2>=3.1",
  "jsonschema>=4.22",
  "pydantic>=2.7",
  "rich>=13.7",
  "typer>=0.12",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
  "ruff>=0.8",
]

[project.scripts]
threadvault = "threadvault.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 140
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
